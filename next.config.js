/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexcels.com",
            "images.pexcels.com",
            "images.pexels.com"
        ]
    }
}

module.exports = nextConfig
